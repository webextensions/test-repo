#!/usr/bin/env node

var path = require('path');

var quickInstall = require('./lib/main.js');

quickInstall({
    argv: process.argv,
    projectRoot: path.resolve(__dirname, '..'),
    // archiveFolder: '/var/tmp/node_modules-archive/archive'   // Use such a path to share this functionality over multiple projects / directories
    archiveFolder: 'node_modules-archive/archive',              // Use such a path to keep the cached node_modules near the project directory itself
    showDiskSpaceWarningBelow: 10 * 1024 * 1024 * 1024          // Warn if free disk space for the partition containing "archiveFolder" is less than 10 GB
});
