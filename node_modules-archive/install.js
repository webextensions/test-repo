#!/usr/bin/env node

var path = require('path');

var runNpmInstall = require('./script.js');

runNpmInstall({
    projectRoot: path.resolve(__dirname, '..'),
    archiveFolderWrtProjectRoot: 'node_modules-archive/archive'
});